# Post-hoc short-lag sensitivity

This fork cannot alter the original four-tau confirmation. It tests whether shorter kernels continue to improve and whether the selected memory feature adds anything beyond an explicit previous-state baseline.

Run the guarded Phase C baseline-enablement patch once, then Phase A depth expansion, Phase B depth screen, lag enrichment, Phase B lag screen, and the recomputed-history Phase C lag null. All outputs use separate directories and `posthoc_sensitivity_only` status.
